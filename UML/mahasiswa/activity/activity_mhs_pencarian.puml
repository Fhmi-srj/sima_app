@startuml Activity Diagram - Pencarian

title Activity Diagram: Pencarian Mahasiswa

start

:Mahasiswa tap FAB Search / Menu Cari;

:Sistem menampilkan halaman pencarian;

:Menampilkan search bar;
:Menampilkan filter chips;
:Menampilkan akses cepat;

note right
  Filter:
  - Semua
  - Jadwal
  - Akademik
  - Keuangan
  - Sertifikat
end note

:Mahasiswa memilih filter (opsional);

:Mahasiswa mengetik kata kunci;

if (Kata kunci >= 2 karakter?) then (ya)
  :Sistem melakukan pencarian real-time;
  
  fork
    :Mencari di data jadwal;
  fork again
    :Mencari di data akademik;
  fork again
    :Mencari di data keuangan;
  fork again
    :Mencari di data sertifikat;
  end fork
  
  :Menggabungkan hasil pencarian;
  
  if (Ada hasil?) then (ya)
    :Menampilkan daftar hasil pencarian;
    
    :Setiap hasil menampilkan:
    - Icon kategori
    - Judul
    - Deskripsi singkat
    - Badge kategori;
    
    repeat
      if (Mahasiswa tap hasil?) then (ya)
        :Sistem navigasi ke halaman terkait;
        
        note right
          Contoh:
          - Jadwal → Detail jadwal
          - Akademik → Detail KRS/KHS
          - Keuangan → Detail tagihan
          - Sertifikat → Detail sertifikat
        end note
        
      else (tidak)
      endif
    repeat while (Lanjut melihat hasil?) is (ya)
    
  else (tidak)
    :Menampilkan icon search_off;
    :Menampilkan pesan "Tidak ditemukan";
    :Menampilkan kata kunci yang dicari;
  endif
  
else (tidak)
  :Menampilkan akses cepat:
  - Jadwal hari ini
  - KRS aktif
  - Tagihan pending
  - Sertifikat terbaru;
endif

:Mahasiswa dapat clear search;

stop

@enduml
